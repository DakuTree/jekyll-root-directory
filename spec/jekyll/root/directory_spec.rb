RSpec.describe Jekyll::Root::Directory do
  it "has a version number" do
    expect(Jekyll::Root::Directory::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
